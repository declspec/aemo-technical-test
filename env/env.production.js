module.exports = {
    urls: {
        // in production the API lives on the same domain as the WebUI
        // so relative paths can be used.
        api: '/api',
        baseHref: '/aemo-technical-test' // GitHub pages
    }
}